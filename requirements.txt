# ONE-TIME bootstrap for qwen-vlm (packages persist in the conda env).
# Do NOT re-run pip on every `conda activate`.
#
#   ./install_env.sh
#
# Or manually (isolated — no PYTHONPATH from other envs):
#   env -i PATH=/root/miniforge3/envs/qwen-vlm/bin:/usr/bin:/bin \
#     /root/miniforge3/envs/qwen-vlm/bin/python -m pip install -r requirements.txt
#
# If imports fail, check first:
#   which python    # must be .../envs/qwen-vlm/bin/python
#   echo $PYTHONPATH   # must NOT list .../envs/sim/...
#   python -c "import qwen_vl_utils"
#
# ROS 2 (rclpy) is installed by install_env.sh via mamba — not on PyPI.

# Qwen2.5-VL-3B-Instruct-AWQ inference stack (legacy autoawq path)
# transformers loads the model; autoawq unpacks AWQ weights internally.
# autoawq last tested with torch 2.6.0 + transformers 4.51.3.

torch==2.6.0
torchvision==0.21.0
transformers==4.51.3
autoawq==0.2.9
accelerate>=1.0.0
qwen-vl-utils>=0.0.14
numpy<2
safetensors>=0.4.0
pillow>=10.0

# ROS 2 bridge for IsaacSimPublisher (conda/mamba only — not on PyPI):
# ros-humble-rclpy
# ros-humble-geometry-msgs
# ros-humble-sensor-msgs
